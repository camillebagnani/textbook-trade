import { useQuery } from '@apollo/client';
// import { QUERY_ME } from '../../utils/queries';

function User() {
  // const { data } = useQuery(QUERY_ME);
  // let user;

  // if (data) {
  //   user = data.me;
  // }
    return (
      <>
      <div>
Hi
      </div>
      </>
    )
  }
  
  export default User;