const User = require('./User');
const Book = require('./Book');
const Transaction = require('./Transaction');
const Subject = require('./Subject');

module.exports = { User, Book, Transaction, Subject };
